# Duality in Mathematics

This exposition was migrated from
`cool-math/notebooks/convex_analysis/convex_duality.ipynb`. The reader-facing
LaTeX source is `main.tex`; notebook-generated figures are stored in `images/`.

From the website repository root, rebuild the PDF with:

```bash
make expositions
```
