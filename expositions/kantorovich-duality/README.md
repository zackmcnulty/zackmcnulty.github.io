# Kantorovich Duality and the Wasserstein Space

The source is in `main.tex`, with shared commands and package imports in
`preamble.tex`. From the repository root, rebuild the PDF with:

```bash
make expositions
```
